<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav">
                <li><a href="/">EPL Central</a></li>
                <li><a href="/teams">Teams</a></li>
                <li><a href="/players">Players</a></li>
                <li><a href="/matches">Matches</a></li>
                <li class="active"><a href="/seasons">Seasons</a></li>
                <li><a href="/champions">Champions</a></li>
            </ul>
        </div>
    </div>
</nav>

<div class="container-fluid text-center">
    <div class="row content">
        <div class="col-sm-2 sidenav">

        </div>
        <div class="col-sm-8 text-left">
            {{!-- Create a table --}}
            <table>

                {{!-- Header section --}}
                <thead>

                    {{!-- For just the first row, we print each key of the row object as a header cell so we
                    know what each column means when the page renders --}}
                    <tr>
                        {{#each data.[0]}}
                        <th>
                            {{@key}}
                        </th>
                        {{/each}}
                    </tr>
                </thead>

                {{!-- Body section --}}
                <tbody>

                    {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
                    {{#each data}}
                    <tr>
                        <td>{{this.id}}</td>
                        <td>{{this.seasonStartDate}}</td>
                        <td>{{this.seasonEndDate}}</td>
                        <td>{{this.matchOfTheSeasonID}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        <div class="col-sm-2 sidenav">
            {{!-- Form to add a record to the table --}}
            <h2>Adding Data with AJAX</h2>
            <p>To add a new EPL season, please enter their information below and click 'Submit'!</p>
            <form id="add-season-form">
                <label for="input-seasonStartDate">Start Date: </label>
                <input type="date" name="input-seasonStartDate" id="input-seasonStartDate">

                <label for="input-seasonEndDate">End Date: </label>
                <input type="date" name="input-seasonEndDate" id="input-seasonEndDate">

                <label for="input-matchOfTheSeasonID">Match of the Season ID: </label>
                <input type="number" name="input-matchOfTheSeasonID" id="input-matchOfTheSeasonID">

                <input type="submit">
            </form>
        </div>
    </div>
</div>